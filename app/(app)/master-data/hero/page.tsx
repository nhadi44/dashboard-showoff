import { ThemeSwitch } from '@/components/theme-switch';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Hero',
};

export default function Hero() {
  return (
    <section>
      <h1>Master Data Hero</h1>
    </section>
  );
}